Kada klonirate projekat, napravite u htdocsu folder epos_zadatak i sve sa githuba prekopirajte tamo, molim Vas da napravite u phpmyadminu bazu koja se zove epos_zadatak i udjeteu SQL, nalepite sledeci kod i izvrsite
CREATE TABLE adminreg (
    admin_id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(255) NOT NULL,
    last_name VARCHAR(255) NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL
);
